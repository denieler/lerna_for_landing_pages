{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "scripts": {
    "test": "lerna run test",
    "changed": "lerna changed",
    "deploy": "lerna exec"
  }
}
